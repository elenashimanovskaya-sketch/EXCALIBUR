Temporary note: put Elena reference photos here for cover generation.
You can delete this note after adding files.

# Elena reference photos

Add 5-15 portrait photos of Elena (jpg/png) into this folder.

Recommended naming:
- `elena-ref-01.jpg`
- `elena-ref-02.jpg`
- `elena-ref-03.jpg`

Then upload those images somewhere public (WordPress Media is best) and copy URLs
to `memory/cover/blog-hero.json` -> `reference_urls_hosted`.

The quad cover batch will use all URLs from `reference_urls_hosted` as `input_urls`.
